type uim, domain;
type uim_exec, exec_type, vendor_file_type, file_type;

init_daemon_domain(uim)

allow uim kernel:system module_request;

allow uim rootfs:lnk_file getattr;
allow uim sysfs:file r_file_perms;

allow uim tty_device:chr_file { ioctl open read write };
